<?xml version="1.0" encoding="UTF-8" ?>

<!--
This file is part of JavaSMT,
an API wrapper for a collection of SMT solvers:
https://github.com/sosy-lab/java-smt

SPDX-FileCopyrightText: 2024 Dirk Beyer <https://www.sosy-lab.org>

SPDX-License-Identifier: Apache-2.0
-->

<!-- vim: set tabstop=8 shiftwidth=4 expandtab sts=4 filetype=ant fdm=marker: -->
<project name="publish-solvers-bitwuzla" basedir="." xmlns:ivy="antlib:org.apache.ivy.ant">

    <property name="bitwuzla.dist.dir" value="${ivy.solver.dist.dir}/bitwuzla"/>

    <import file="macros.xml"/>

    <target name="bitwuzla-initialize">
        <checkPathOption pathOption="bitwuzla.path" defaultPath="/path/to/bitwuzla" targetName="Bitwuzla source folder (git checkout from 'https://github.com/bitwuzla/bitwuzla)"/>
        <fail unless="bitwuzla.customRev">
            Please specify a custom revision with the flag -Dbitwuzla.customRev=XXX.
            The custom revision should be a version number of Bitwuzla.
            The script will append the git revision.
        </fail>

        <!-- TODO Change these paths when building outside of the container. We need static version of gmp and mpfr with `-fPIC` enabled -->
        <property name="jdk-linux-arm64.path" value="/dependencies/jdk17-linux-aarch64"/>
        <property name="jdk-windows-x64.path" value="/dependencies/jdk11-windows-x64"/>

        <property name="gmp.x64-linux" location="/dependencies/gmp-6.3.0/install/x64-linux/"/>
        <property name="gmp.x64-windows" location="/dependencies/gmp-6.3.0/install/x64-windows/"/>
        <property name="gmp.arm64-linux" location="/dependencies/gmp-6.3.0/install/arm64-linux/"/>

        <property name="mpfr.x64-linux" location="/dependencies/mpfr-4.2.2/install/x64-linux/"/>
        <property name="mpfr.x64-windows" location="/dependencies/mpfr-4.2.2/install/x64-windows/"/>
        <property name="mpfr.arm64-linux" location="/dependencies/mpfr-4.2.2/install/arm64-linux/"/>

        <!-- get a naive version -->
        <exec executable="git" dir="${bitwuzla.path}" outputproperty="bitwuzla.revision"
              failonerror="true">
            <arg value="show"/>
            <arg value="-s"/>
            <arg value="--format=%h"/>
        </exec>

        <!-- set properties for the next steps -->
        <property name="bitwuzla.version" value="${bitwuzla.customRev}-g${bitwuzla.revision}"/>
        <property name="install.x64-linux" location="${bitwuzla.path}/install/x64-linux"/>
        <property name="install.x64-windows" location="${bitwuzla.path}/install/x64-windows"/>
        <property name="install.arm64-linux" location="${bitwuzla.path}/install/arm64-linux"/>

        <property name="source.path" location="lib/native/source"/>
    </target>

    <target name="bitwuzla-generate-jni" description="Generate JNI headers for Bitwuzla">
        <checkPathOption pathOption="bitwuzla.path" defaultPath="/path/to/bitwuzla" targetName="Bitwuzla source folder (git checkout from 'https://github.com/bitwuzla/bitwuzla)"/>

        <property name="source.path" location="lib/native/source"/>

        <!-- Create output directory for the swig proxies -->
        <delete dir="${source.path}/libbitwuzla/src" quiet="true"/>
        <mkdir dir="${source.path}/libbitwuzla/src/org/sosy_lab/java_smt/solvers/bitwuzla/api"/>

        <!-- Run swig to generate java files and the c wrapper -->
        <exec executable="swig" dir="${source.path}/libbitwuzla" failonerror="true">
            <arg value="-java"/>
            <arg value="-c++"/>
            <arg value="-I${bitwuzla.path}/include"/>
            <arg value="-package"/>
            <arg value="org.sosy_lab.java_smt.solvers.bitwuzla.api"/>
            <arg value="-outdir"/>
            <arg value="src/org/sosy_lab/java_smt/solvers/bitwuzla/api"/>
            <arg value="-o"/>
            <arg value="bitwuzla_wrap.cpp"/>
            <arg value="bitwuzla.i"/>
        </exec>

        <!-- Store the unpatched bindings -->
        <copy todir="${source.path}/libbitwuzla.nopatch">
            <fileset dir="${source.path}/libbitwuzla"/>
        </copy>

        <!-- Patch the bindings -->
        <exec executable="patch" failonerror="true">
            <arg value="-p0"/>
            <arg value="--no-backup-if-mismatch"/>
            <arg value="-i"/>
            <arg value="${source.path}/libbitwuzla/swigWrapper.patch"/>
        </exec>
    </target>

    <target name="bitwuzla-build-jni" depends="bitwuzla-initialize">
        <!-- Check that the swig bindings were regenerated -->
        <condition property="swig.regenerated">
            <available file="${source.path}/libbitwuzla.nopatch" type="dir"/>
        </condition>
        <fail unless="swig.regenerated">
            Please regenerate the jni bindings first
        </fail>

        <delete dir="${bitwuzla.path}/install/java"/>
        <delete dir="${bitwuzla.dist.dir}"/>

        <!-- Compile java proxies and create jar file -->
        <mkdir dir="${bitwuzla.path}/install/java/build"/>
        <javac release="11" debug="true" srcdir="${source.path}/libbitwuzla/src/" destdir="${bitwuzla.path}/install/java/build" includeantruntime="false" failonerror="true">
            <include name="org/sosy_lab/java_smt/solvers/bitwuzla/api/*.java"/>
        </javac>
        <jar destfile="${bitwuzla.dist.dir}/bitwuzla-${bitwuzla.version}.jar" basedir="${bitwuzla.path}/install/java/build"/>

        <!-- Generate and package javadoc documentation -->
        <mkdir dir="${bitwuzla.path}/install/java/doc"/>
        <javadoc sourcepath="${source.path}/libbitwuzla/src" destdir="${bitwuzla.path}/install/java/doc" failonerror="true"/>
        <jar destfile="${bitwuzla.dist.dir}/bitwuzla-${bitwuzla.version}-javadoc.jar" basedir="${bitwuzla.path}/install/java/doc"/>

        <!-- Package swig generated source code -->
        <jar destfile="${bitwuzla.dist.dir}/bitwuzla-${bitwuzla.version}-sources.jar" basedir="${source.path}/libbitwuzla/src"/>

        <!-- Create a new patch -->
        <exec executable="diff">
            <redirector output="${bitwuzla.path}/install/java/swig.patch"/>
            <arg value="-ur"/>
            <arg value="--unidirectional-new-file"/>
            <arg value="lib/native/source/libbitwuzla.nopatch"/>
            <arg value="lib/native/source/libbitwuzla"/>
        </exec>

        <!-- Copy the patch to the source tree and fix the paths-->
        <copy file="${bitwuzla.path}/install/java/swig.patch" tofile="${source.path}/libbitwuzla/swigWrapper.patch"/>
        <exec executable="sed">
            <arg value="-i"/>
            <arg value="s/libbitwuzla.nopatch/libbitwuzla/g"/>
            <arg value="${source.path}/libbitwuzla/swigWrapper.patch"/>
        </exec>

        <!-- Delete libbitwuzla.nopatch folder -->
        <delete dir="${source.path}/libbitwuzla.nopatch"/>
    </target>

    <target name="bitwuzla-build-linux-x64" depends="bitwuzla-build-jni, bitwuzla-initialize">
        <delete dir="${install.x64-linux}" quiet="true"/>
        <echo message="Building Bitwuzla in version '${bitwuzla.version}' for Linux x64"/>

        <!-- configure and build Bitwuzla -->
        <exec executable="./configure.py" dir="${bitwuzla.path}" failonerror="true">
            <env key="PKG_CONFIG_PATH" value="build/:${gmp.x64-linux}/lib/pkgconfig/:${mpfr.x64-linux}/lib/pkgconfig/"/>
            <arg value="--static"/>
            <arg value="--wipe"/>
            <arg value="--fpexp"/>
            <arg value="--prefix"/>
            <arg value="${install.x64-linux}"/>
        </exec>
        <exec executable="ninja" dir="${bitwuzla.path}/build/" failonerror="true">
            <arg value="install"/>
        </exec>

        <!-- compile the swig wrapper -->
        <exec executable="g++" dir="${source.path}/libbitwuzla" failonerror="true">
            <arg value="-fPIC"/>
            <arg value="-c"/>
            <arg value="bitwuzla_wrap.cpp"/>
            <arg value="-I${bitwuzla.path}/include"/>
            <arg value="-I${install.x64-linux}/include"/>
            <arg value="-I${java.home}/include"/>
            <arg value="-I${java.home}/include/linux"/>
        </exec>

        <!-- link the wrapper to create libbitwuzlaj-x64.so -->
        <!-- Licensing:
             GMP is licensed under LGPL:
              - https://www.gnu.org/licenses/lgpl-3.0.html
             Bitwuzla is licensed under MIT license:
              - https://github.com/bitwuzla/bitwuzla/blob/main/COPYING
             Based on that information, we provide the whole library including its dependencies under the MIT license.
        -->
        <exec executable="g++" dir="${source.path}/libbitwuzla" failonerror="true">
            <arg value="-shared"/>
            <arg value="-o"/>
            <arg value="libbitwuzlaj-x64.so"/>
            <arg value="bitwuzla_wrap.o"/>
            <arg value="-Wl,--whole-archive"/>
            <arg value="${install.x64-linux}/lib/x86_64-linux-gnu/libbitwuzla.a"/>
            <arg value="${install.x64-linux}/lib/x86_64-linux-gnu/libbitwuzlabb.a"/>
            <arg value="${install.x64-linux}/lib/x86_64-linux-gnu/libbitwuzlabv.a"/>
            <arg value="${install.x64-linux}/lib/x86_64-linux-gnu/libbitwuzlals.a"/>
            <arg value="${gmp.x64-linux}/lib/libgmp.a"/>
            <arg value="${gmp.x64-linux}/lib/libgmpxx.a"/>
            <arg value="${mpfr.x64-linux}/lib/libmpfr.a"/>
            <arg value="-Wl,--no-whole-archive,--allow-multiple-definition"/>
            <arg value="-Wl,-z,defs"/>
        </exec>

        <exec executable="strip" dir="${source.path}/libbitwuzla" failonerror="true">
            <arg value="libbitwuzlaj-x64.so"/>
        </exec>
    </target>

    <target name="bitwuzla-build-linux-arm64" depends="bitwuzla-build-jni">
        <delete dir="${install.arm64-linux}" quiet="true"/>
        <echo message="Building Bitwuzla in version '${bitwuzla.version}' for Linux arm64"/>

        <!-- configure and build Bitwuzla -->
        <exec executable="./configure.py" dir="${bitwuzla.path}" failonerror="true">
            <env key="PKG_CONFIG_PATH" value="build/:${gmp.arm64-linux}/lib/pkgconfig/:${mpfr.arm64-linux}/lib/pkgconfig/"/>
            <arg value="--arm64"/>
            <arg value="--static"/>
            <arg value="--wipe"/>
            <arg value="--fpexp"/>
            <arg value="--prefix"/>
            <arg value="${install.arm64-linux}"/>
        </exec>
        <exec executable="ninja" dir="${bitwuzla.path}/build/" failonerror="true">
            <arg value="install"/>
        </exec>

        <!-- compile the swig wrapper -->
        <exec executable="aarch64-linux-gnu-g++" dir="${source.path}/libbitwuzla" failonerror="true">
            <arg value="-fPIC"/>
            <arg value="-c"/>
            <arg value="bitwuzla_wrap.cpp"/>
            <arg value="-I${bitwuzla.path}/include"/>
            <arg value="-I${install.arm64-linux}/include"/>
            <arg value="-I${jdk-linux-arm64.path}/include"/>
            <arg value="-I${jdk-linux-arm64.path}/include/linux"/>
        </exec>

        <!-- link the wrapper to create libbitwuzlaj-arm64.so -->
        <!-- Licensing:
             GMP is licensed under LGPL:
              - https://www.gnu.org/licenses/lgpl-3.0.html
             Bitwuzla is licensed under MIT license:
              - https://github.com/bitwuzla/bitwuzla/blob/main/COPYING
             Based on that information, we provide the whole library including its dependencies under the MIT license.
        -->
        <exec executable="aarch64-linux-gnu-g++" dir="${source.path}/libbitwuzla" failonerror="true">
            <arg value="-shared"/>
            <arg value="-o"/>
            <arg value="libbitwuzlaj-arm64.so"/>
            <arg value="bitwuzla_wrap.o"/>
            <arg value="-Wl,--whole-archive"/>
            <arg value="${install.arm64-linux}/lib/libbitwuzla.a"/>
            <arg value="${install.arm64-linux}/lib/libbitwuzlabb.a"/>
            <arg value="${install.arm64-linux}/lib/libbitwuzlabv.a"/>
            <arg value="${install.arm64-linux}/lib/libbitwuzlals.a"/>
            <arg value="${gmp.arm64-linux}/lib/libgmp.a"/>
            <arg value="${gmp.arm64-linux}/lib/libgmpxx.a"/>
            <arg value="${mpfr.arm64-linux}/lib/libmpfr.a"/>
            <arg value="-Wl,--no-whole-archive,--allow-multiple-definition"/>
            <arg value="-Wl,-z,defs"/>
        </exec>

        <exec executable="aarch64-linux-gnu-strip" dir="${source.path}/libbitwuzla" failonerror="true">
            <arg value="libbitwuzlaj-arm64.so"/>
        </exec>
    </target>

    <target name="bitwuzla-build-windows-x64" depends="bitwuzla-build-jni">
        <delete dir="${install.x64-windows}" quiet="true"/>
        <echo message="Building Bitwuzla in version '${bitwuzla.version}' for Windows"/>

        <!-- configure and build Bitwuzla -->
        <exec executable="./configure.py" dir="${bitwuzla.path}" failonerror="true">
            <env key="PKG_CONFIG_PATH" value="build/:${gmp.x64-windows}/lib/pkgconfig/:${mpfr.x64-windows}/lib/pkgconfig/"/>
            <arg value="--win64"/>
            <arg value="--static"/>
            <arg value="--wipe"/>
            <arg value="--fpexp"/>
            <arg value="--prefix"/>
            <arg value="${install.x64-windows}"/>
        </exec>
        <exec executable="ninja" dir="${bitwuzla.path}/build/" failonerror="true">
            <arg value="install"/>
        </exec>

        <!-- compile the swig wrapper -->
        <exec executable="x86_64-w64-mingw32-g++" dir="${source.path}/libbitwuzla" failonerror="true">
            <arg value="-std=c++17"/>
            <arg value="-fPIC"/>
            <arg value="-c"/>
            <arg value="bitwuzla_wrap.cpp"/>
            <arg value="-I${bitwuzla.path}/include"/>
            <arg value="-I${install.x64-windows}/include"/>
            <arg value="-I${jdk-windows-x64.path}/include"/>
            <arg value="-I${jdk-windows-x64.path}/include/win32"/>
        </exec>

        <!-- link the wrapper to create libbitwuzlaj-x64.dll -->
        <!-- Note:
             libwinpthread is required when using mingw-posix-threads.
             We do not want to install mingw or cygwin on the target-system.
        -->
        <!-- Licensing:
             GCC-based libgcc and libstd++ is licensed under GPL,
             with an explicit allowance for combining with non-GPL-libraries:
              - https://www.gnu.org/licenses/gcc-exception-3.1-faq.en.html
              - https://gcc.gnu.org/onlinedocs/libstdc++/manual/license.html
             Winpthreads is licensed under MIT:
              - https://sourceforge.net/p/mingw-w64/mingw-w64/ci/master/tree/mingw-w64-libraries/winpthreads/COPYING
             GMP is licensed under LGPL:
              - https://www.gnu.org/licenses/lgpl-3.0.html
             Bitwuzla is licensed under MIT license:
              - https://github.com/bitwuzla/bitwuzla/blob/main/COPYING
             Based on that information, we provide the whole library including its dependencies under the MIT license.
        -->
        <exec executable="x86_64-w64-mingw32-g++" dir="${source.path}/libbitwuzla" failonerror="true">
            <arg value="-std=c++17"/>
            <arg value="-shared"/>
            <arg value="-o"/>
            <arg value="libbitwuzlaj-x64.dll"/>
            <arg value="bitwuzla_wrap.o"/>
            <arg value="-Wl,--whole-archive"/>
            <arg value="/usr/x86_64-w64-mingw32/lib/libwinpthread.a"/>
            <arg value="${install.x64-windows}/lib/libbitwuzlabb.a"/>
            <arg value="${install.x64-windows}/lib/libbitwuzlabv.a"/>
            <arg value="${install.x64-windows}/lib/libbitwuzlals.a"/>
            <arg value="${install.x64-windows}/lib/libbitwuzla.a"/>
            <arg value="${gmp.x64-windows}/lib/libgmp.a"/>
            <arg value="${gmp.x64-windows}/lib/libgmpxx.a"/>
            <arg value="${mpfr.x64-windows}/lib/libmpfr.a"/>
            <arg value="-Wl,--no-whole-archive"/>
            <arg value="-static-libgcc"/>
            <arg value="-static-libstdc++"/>
            <arg value="-lpsapi"/>
            <arg value="-Wl,--allow-multiple-definition"/>
        </exec>

        <exec executable="strip" dir="${source.path}/libbitwuzla" failonerror="true">
            <arg value="libbitwuzlaj-x64.dll"/>
        </exec>
    </target>

    <target name="publish-bitwuzla"
            depends="bitwuzla-initialize, bitwuzla-build-linux-x64, bitwuzla-build-linux-arm64, bitwuzla-build-windows-x64, load-ivy"
            description="Publish Bitwuzla binaries to Ivy repository.">
        <!-- copy library files into directory to be published for Ivy -->
        <copy file="${source.path}/libbitwuzla/libbitwuzlaj-x64.so" tofile="${bitwuzla.dist.dir}/x64/libbitwuzlaj-${bitwuzla.version}.so" failonerror="true"/>
        <copy file="${source.path}/libbitwuzla/libbitwuzlaj-arm64.so" tofile="${bitwuzla.dist.dir}/arm64/libbitwuzlaj-${bitwuzla.version}.so" failonerror="true"/>
        <copy file="${source.path}/libbitwuzla/libbitwuzlaj-x64.dll" tofile="${bitwuzla.dist.dir}/x64/libbitwuzlaj-${bitwuzla.version}.dll" failonerror="true"/>

        <ivy:resolve conf="solver-bitwuzla" file="solvers_ivy_conf/ivy_bitwuzla.xml"/>
        <publishToRepository solverName="Bitwuzla" solverVersion="${bitwuzla.version}" distDir="${bitwuzla.dist.dir}"/>

        <!--
          We additionally provide x64-files without arch-attribute for backwards compatibility,
          such that applications can load dependencies without changing their Ivy configuration.
          Those files are not part of any direct configuration, but will be resolved if the
          arch-attribute is not used.
        -->
        <echo>
            Lets copy the files for architecture x64 into main directory, for backwards compatibility.
            Afterward, please execute the SVN command from above.
        </echo>
        <copy todir="repository/${ivy.organisation}/${ivy.module}/" verbose="true">
            <fileset dir="repository/${ivy.organisation}/${ivy.module}/x64/">
                <include name="*-${bitwuzla.version}.*"/>
            </fileset>
        </copy>
    </target>
</project>
